# NCAA Wrestling Tracker — Environment Variables
# ================================================
#
# For local development: leave these unset to use local pin_cache/ folder.
# For Posit Connect: set these in each content item's Environment settings.
#
# CONNECT_SERVER is auto-injected by Posit Connect at runtime.
# CONNECT_API_KEY must be set manually in content settings.

# Posit Connect server URL (auto-injected on Connect)
# CONNECT_SERVER=https://your-connect-server.posit.co

# Your Posit Connect API key (generate at Connect → API Keys)
# CONNECT_API_KEY=your-api-key-here
